version: "3"

services: 
  api:
    build:
      context: .
      dockerfile: operations/docker/Dockerfile
    ports: 
      - "1337:1337"

  mysql:
    image: mysql
    environment:
      - MYSQL_DATABASE=todo
      - MYSQL_USER=api
      - MYSQL_PASSWORD=test
      - MYSQL_ROOT_PASSWORD=test
